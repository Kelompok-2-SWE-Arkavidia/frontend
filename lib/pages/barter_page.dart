import 'package:flutter/material.dart';

class BarterPage extends StatelessWidget {
  const BarterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Barter')),
      body: const Center(child: Text('Halaman Barter')),
    );
  }
}
